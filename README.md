A simple Python CLI tool to pick Bingo entries and load them into the OS clipboard one by one.

`entries.txt` stores each entry on a line. Prefilled with some Bridgerton Bingo entries!

Just run `script.py`